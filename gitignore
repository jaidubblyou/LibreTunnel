# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode (in case anyone opens this via a generated project)
DerivedData/
*.xcuserstate
xcuserdata/

# Release packaging output
dist/

# Delivered archives shouldn't be committed inside the repo they contain
*.zip

# macOS
.DS_Store
